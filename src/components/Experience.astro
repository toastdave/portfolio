<section id="experience" class="experience">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Work Experience</h2>
      <p class="section-subtitle">My professional journey</p>
    </div>
    
    <div class="experience-timeline">
      <div class="timeline">
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h4 class="timeline-title">Software Engineer II</h4>
            <div class="timeline-info">
              <span class="timeline-company">CAPTRUST</span>
              <span class="timeline-date">December 2024 - Present</span>
            </div>
            <p class="timeline-description">
              Led development of cross-company initiative to link lead generation platform and automate full internal client experience
            </p>
            <p class="timeline-description">
              Migrated department codebase to templated monorepos for shared projects
            </p>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h4 class="timeline-title">Software Engineer I</h4>
            <div class="timeline-info">
              <span class="timeline-company">CAPTRUST</span>
              <span class="timeline-date">January 2023 - December 2024</span>
            </div>
            <p class="timeline-description">
              Developed scalable backend services (APIs, Serverless Functions, WebJobs) using C# .Net, Azure, and SQL
            </p>
            <p class="timeline-description">
              Revamped department CI/CD to templated YAML pipelines
            </p>
            <p class="timeline-description">
              Built a Next.js frontend for department deployment automation
            </p>
            <p class="timeline-description">
              Setup QA testing framework with Playwright
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .experience {
    padding: var(--space-7) 0;
    color: var(--theme-text, var(--color-text));
    position: relative;
    overflow: hidden;
  }
  
  .experience::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at top right, rgba(var(--color-primary-rgb), 0.1), transparent 50%),
                radial-gradient(circle at bottom left, rgba(var(--color-accent-rgb), 0.1), transparent 50%);
    z-index: 0;
  }
  
  .container {
    position: relative;
    z-index: 1;
  }
  
  .section-header {
    text-align: center;
    margin-bottom: var(--space-5);
  }
  
  .section-title {
    font-size: 2.5rem;
    margin-bottom: var(--space-2);
    position: relative;
    display: inline-block;
    color: var(--theme-text, var(--color-text));
  }
  
  .section-title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 3px;
    background: linear-gradient(90deg, var(--color-primary-600), var(--color-accent-500));
    border-radius: var(--radius-full);
  }
  
  .section-subtitle {
    color: var(--theme-text-secondary, var(--color-text-secondary));
    font-size: 1.1rem;
    max-width: 600px;
    margin: 0 auto;
  }
  
  .experience-timeline {
    margin-top: var(--space-4);
  }
  
  .timeline {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
  }
  
  .timeline::before {
    content: '';
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    width: 2px;
    background: linear-gradient(to bottom, var(--color-primary-600), var(--color-accent-500));
    margin-left: 30px;
  }
  
  .timeline-item {
    display: flex;
    margin-bottom: var(--space-4);
    position: relative;
  }
  
  .timeline-marker {
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background-color: var(--color-primary-600);
    border: 3px solid var(--theme-bg, var(--color-bg));
    margin-top: 5px;
    margin-right: var(--space-3);
    flex-shrink: 0;
    position: relative;
    z-index: 1;
    margin-left: 23px;
  }
  
  .timeline-content {
    padding: var(--space-3);
    flex: 1;
  }
  
  .timeline-content h4 {
    margin-bottom: var(--space-1);
    font-size: 1.1rem;
    color: var(--theme-text, var(--color-text));
  }
  
  .timeline-info {
    display: flex;
    justify-content: space-between;
    font-size: 0.9rem;
    margin-bottom: var(--space-2);
  }
  
  .timeline-company {
    font-weight: 500;
    color: var(--color-primary-600);
  }
  
  .timeline-date {
    font-weight: 500;
    color: var(--color-text-secondary);
  }
  
  .timeline-description {
    color: var(--theme-text-secondary, var(--color-text-secondary));
    font-size: 0.95rem;
    margin-bottom: var(--space-2);
  }
  
  .timeline-description:last-child {
    margin-bottom: 0;
  }
  
  /* Theme variables for light/dark mode */
  :root {
    --theme-text: var(--color-text);
    --theme-text-secondary: var(--color-text-secondary);
    --theme-bg: var(--color-bg);
    --theme-card-bg: var(--color-bg-secondary);
    --theme-border: rgba(0, 0, 0, 0.1);
  }
  
  [data-theme="dark"] {
    --theme-text: var(--color-text-light, #ffffff);
    --theme-text-secondary: var(--color-text-secondary-light, #a0a0a0);
    --theme-bg: var(--color-bg-dark, #121212);
    --theme-card-bg: rgba(255, 255, 255, 0.05);
    --theme-border: rgba(255, 255, 255, 0.1);
  }
  
  @media (max-width: 768px) {
    .section-title {
      font-size: 2rem;
    }
    
    .timeline::before {
      margin-left: 20px;
    }
    
    .timeline-marker {
      width: 12px;
      height: 12px;
      margin-left: 17px;
    }
    
    .timeline-info {
      flex-direction: column;
      gap: var(--space-1);
    }
  }
</style> 